import React, { Fragment } from 'react';
import CatalogContainer from '../../../containers/CatalogContainer';

export default () => (
  <Fragment>
    <h1>
      Products
    </h1>
    <CatalogContainer />
  </Fragment>
);
